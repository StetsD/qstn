
function reversePrint(linkedList) {
  // ...
}

var someList = {
  value: 1,
  next: {
    value: 2,
    next: {
      value: 3,
      next: {
        value: 4,
        next: null
      }
    }
  }
};

reversePrint(someList);

// Написать код функции reversePrint(), которая выведет значения переданного ей односвязного списка в обратном порядке (4, 3, 2, 1).
// Можно предложить больше одного варианта реализации, в таком случае сравнить и описать плюсы и минусы каждого варианта.